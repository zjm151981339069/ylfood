$(function() {
	$("#content").css('width',$("#content").width()-6);
});

//返回按钮
function back(){
	var searchStr = $("#searchConditionForm").serialize();
	window.location.href='../exchange/list?'+searchStr;
}